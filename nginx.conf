server {
    listen 80;
    server_name 111.88.150.188;

    location / {
        proxy_pass http://web:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static/ {
        proxy_pass http://web:8000;
    }

    location /media/ {
        alias /app/media/;
    }
}
